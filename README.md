# Постановка задачи
Требуется разработать Web приложение - личный кабинет пользователя ML сервиса. Подборка примеров с подобными сервисами - ссылка.

 

## Функциональные требования к личному кабинету

- Главная страница с описанием возможностей ML сервиса
- Возможность зарегистрироваться и авторизоваться
- Возможность просматривать баланс (баланс в условных кредитах)
- Возможность пополнять баланс через кнопку пополнить (эквайринг подключать не требуется)
- Возможность выполнить запрос к ML сервису из Web интерфейса со списанием кредитов с баланса (при условии успешного выполнения запроса)
- Возможность просматривать историю загруженных данных и предсказаний по ним, с указанием суммы списанных кредитов
- Возможность выполнять запросы к ML сервису с помощью REST интерфейса
- Требуется предусмотреть валидацию данных. Если в загружаемой выборке присутствуют ошибочные данные, то возвращать их пользователю, а над валидными данными производить предсказание
- Требуется предусмотреть проверку на положительный баланс при запросах к системе.
- Роль администратор подразумевает возможность пополнять баланс пользователям (модерировать пополнения от пользователей) и просматривать все транзакции. Данное задание является не обязательным и в случае выполнения принесет дополнительные балы.
 

## Этапы выполнения задачи

- Спроектировать объектную модель сервиса. Выделить базовые сущности с которыми будет происходить работа. Пользователь, ML модель, история транзакций/предсказаний, задача для ML модели и т.д. При проектирование написать названия классов, названия полей, методов, принимаемых типов данных, модификаторов доступа. При проектирование использовать все базовые принципы ООП, а именно инкапсуляцию, наследование для расширения классов и полиморфизм. Проектирование допускается в виде написанного кода.
- Подключить к объектной модели приложения базу данных, с использованием ORM фреймворка. Протестировать работоспособность системы, а именно создание пользователей, пополнение баланса, списание с баланса, история транзакций и т.д. Подготовить сценарий инициализации базы данных стандартными данными (демо пользователь, демо администратор, базовые модели доступные для работы).
- Реализовать 2 интерфейса взаимодействия с системой, REST и Telegram bot. Интерфейс должен содержать весь базовый функционал системы: регистрация, авторизация, отправка данных для предсказания, просмотр и пополнение баланса. 
- Реализовать взаимодействие системы с ML сервисом посредством передачи сообщений. Требуется развернуть несколько воркеров, подключить их к RabbitMQ, настроить RabbitMQ в режиме один издатель - несколько слушателей. Воркеры должны получать ML задачу, проводить валидацию данных, выполнять предикт, сообщать результат своей работы. Требуется протестировать работу системы на примере Telegram bot. 
- Требуется разработать Web приложение, которое будет взаимодействовать с ранее построенной системой и реализует весь требуемый функционал. 
- Требуется покрыть ядро системы тестами. Создание пользователей, постановка ML задачи на выполнение, списание баланса и т.д.
- Требуется упаковать все элементы системы в Docker контейнер и развернуть все локально или на внешнем сервере. 
- Требуется настроить систему мониторинга Web приложения, telegram bot, очередей сообщений, работоспособности системы и провести нагрузочное тестирование. 



------

# Задание №1

- Спроектировать объектную модель сервиса.
- Выделить базовые сущности с которыми будет происходить работа. Пользователь, ML модель, история транзакций/предсказаний, задача для ML модели и т.д.
- При проектирование написать названия классов, названия полей, методов, принимаемых типов данных, модификаторов доступа.
- При проектирование использовать все базовые принципы ООП, а именно инкапсуляцию, наследование для расширения классов и полиморфизм.
- Проектирование допускается в виде написанного кода.



## Практическое задание №1: Проектирование объектной модели ML-сервиса
Контекст задания
Данное задание является отправной точкой всего курса. На этом этапе требуется не писать полноценное приложение, а спроектировать объектную модель сервиса, опираясь на требования общего задания курса.

Результат этого задания будет использоваться и переосмысливаться во всех последующих работах:

при реализации backend;
при работе с ML-задачами;
при проектировании Web-интерфейса;
при построении асинхронного взаимодействия сервисов.
Цель задания
Цель работы — научиться:

выделять ключевые доменные сущности из бизнес-требований;
проектировать объектную модель, отражающую реальную бизнес-логику;
применять базовые принципы ООП (инкапсуляция, наследование, полиморфизм);
формировать основу для дальнейшей реализации системы.
Общие требования к проектированию
При проектировании объектной модели необходимо:

опираться исключительно на требования общего задания курса;
не добавлять сущности «на будущее», не обоснованные требованиями;
использовать понятные и осмысленные названия классов и полей;
явно указывать:поля классов;методы;типы данных;модификаторы доступа.
Проектирование допускается и рекомендуется выполнять в виде кода (например, Python).

Базовые сущности предметной области
Минимальный набор сущностей, который должен быть отражён в объектной модели:

Пользователь;
ML-модель;
Задача для ML-модели;
Результат предсказания;
История ML-запросов.
Допускается объединение или декомпозиция сущностей, если это логически обосновано.

Рекомендуемая структура объектной модели
Пользователь
идентификатор пользователя;
данные для авторизации;
роль пользователя;
баланс;
методы работы с балансом.
ML-модель
идентификатор модели;
описание модели;
стоимость одного предсказания;
метод выполнения предсказания.
ML-задача
входные данные;
статус выполнения;
ссылка на пользователя;
ссылка на ML-модель;
метод запуска обработки.
Транзакции
тип транзакции;
сумма;
дата и время;
связанный пользователь;
связанная ML-задача (если применимо).
Пример оформления (фрагмент)

```py
class Transaction:
def __init__(self, amount: float):
self._amount = amount


def apply(self):
raise NotImplementedError




class DebitTransaction(Transaction):
def apply(self):
# списание средств
pass
```

Пример приведён только для ориентира и не является обязательным шаблоном.

Пошаговый план выполнения задания
- Шаг 1. Анализ требований
Внимательно изучить общее задание курса;
Выписать все бизнес-сущности и действия пользователя.
- Шаг 2. Выделение сущностей
Определить основные классы;
Определить их ответственность.
- Шаг 3. Проектирование классов
Описать поля и методы;
Указать типы данных;
Продумать модификаторы доступа.
- Шаг 4. Применение ООП-принципов
Выделить базовые и производные классы;
Продумать точки расширения;
Реализовать полиморфное поведение.
- Шаг 5. Проверка модели
Проверить, что модель покрывает все требования;
Убедиться, что модель не противоречит бизнес-логике.

Критерии оценки
- Полнота покрытия требований общего задания;
- Корректное применение принципов ООП;
- Логичность и связность объектной модели;
- Читаемость и аккуратность оформления;
- Отсутствие избыточных или дублирующих сущностей.

Результат:
спроектированная объектная модель ML-сервиса, которая может быть напрямую использована как основа для дальнейшей реализации системы.



------

# Задание №2
Требуется организовать структуру проекта в соответствие с примером разобранном на лекции. А также написать docker-compose файл который будет удовлетворять следующим требованиям:

- Требуется создать 4 сервиса - app, web-proxy, rabbitmq, database
- Конфигурирование сервиса app происходит через переменные окружения - env_file
- Исходные файлы сервиса app подключаются через volumes
- Сервис web-proxy создан на основе образа Nginx
- Сервис web-proxy зависит от сервиса app
- Сервис web-proxy пробрасывает наружу порты 80 и 443
- Сервис rabbitmq создается на основе образа rabbitmq
- Сервис rabbitmq пробрасывает наружу порты 15672 и 5672 
- К сервису rabbitmq примонтирован раздел с жесткого диска к разделу /var/lib/rabbitmq внутри контейнера. Для сохранения очереди после перезагрузки сервиса.
- У сервиса rabbitmq установлен параметр - перезагружаться при сбоях 
- Сервис database создан на основе образа postgres
- К сервису database примонтирован раздел локальной операционной системы для сохранения базы данных в случае удаления контейнера с базой


## Практическое задание №2: Структура проекта и Docker Compose
Контекст задания
Данное задание выполняется на раннем этапе курса и закладывает основу для всех последующих работ. Его цель — привести проект к единому, воспроизводимому виду, пригодному для разработки, запуска и масштабирования.

Студенты должны опираться на:
- пример структуры проекта, разобранный на лекции;
- требования общего задания курса;
- принципы изоляции сервисов и конфигурации через окружение.

Цель работы — научиться:
- правильно структурировать backend-проект;
- описывать инфраструктуру приложения с помощью docker-compose;
- разделять ответственность между сервисами;
- обеспечивать воспроизводимый запуск системы.

После выполнения задания студент должен понимать:
- роль каждого сервиса в системе;
- как сервисы взаимодействуют друг с другом;
- как хранить данные и конфигурацию вне контейнеров.

Требования к структуре проекта -  Структура проекта должна соответствовать примеру, разобранному на лекции.

Минимально ожидаемая структура (пример):

project-root/

├── app/

│ ├── src/

│ ├── Dockerfile

│ └── .env

├── web-proxy/

│ ├── nginx.conf

│ └── Dockerfile

├── docker-compose.yml

└── README.md

Допускаются отличия в деталях, если логика структуры сохранена.

Общая архитектура сервисов
В рамках задания требуется поднять 4 сервиса:
- app — backend-приложение;
- web-proxy — reverse proxy (Nginx);
- rabbitmq — брокер сообщений;
- database — база данных PostgreSQL.

Все сервисы должны быть описаны в одном docker-compose.yml файле.

Требования к сервисам
1. Сервис app

Требования:
конфигурирование через переменные окружения;
использование env_file для передачи настроек;
исходные файлы приложения подключаются через volumes;
сервис не должен пробрасывать порты наружу напрямую;
взаимодействие с внешним миром происходит через web-proxy.

2. Сервис web-proxy

Требования:
создаётся на основе образа Nginx;
зависит от сервиса app (depends_on);
проксирует HTTP-запросы к app;
пробрасывает наружу порты:80;443;
конфигурация Nginx хранится в проекте.

3. Сервис rabbitmq

Требования:
создаётся на основе образа rabbitmq;
пробрасывает наружу порты:5672 (AMQP);15672 (management UI);
используется volume для хранения данных:локальная директория → /var/lib/rabbitmq внутри контейнера;
данные очередей должны сохраняться после перезапуска;
установлен параметр автоматического перезапуска при сбоях (restart).

4. Сервис database

Требования:
создаётся на основе образа postgres;
конфигурация через переменные окружения;
используется volume для хранения данных базы;
данные должны сохраняться при удалении контейнера.
Требования к docker-compose.yml
Файл docker-compose.yml должен:

описывать все 4 сервиса;
использовать depends_on для описания зависимостей;
использовать volumes для хранения данных;
использовать env_file для сервиса app;
быть валидным и запускаться командой:
docker compose up

Пошаговый план выполнения задания
- Шаг 1. Создание структуры проекта
Создать директории сервисов;
Подготовить базовые Dockerfile (если требуется).
- Шаг 2. Описание сервиса app
Настроить env_file;
Подключить volumes;
Проверить запуск контейнера.
- Шаг 3. Настройка web-proxy
Подготовить конфигурацию Nginx;
Настроить проксирование на app;
Проверить доступ по порту 80.
- Шаг 4. Подключение RabbitMQ
Добавить сервис rabbitmq;
Настроить проброс портов;
Проверить доступ к management UI.
- Шаг 5. Подключение базы данных
Добавить сервис database;
Настроить volume для хранения данных;
Проверить, что данные сохраняются после перезапуска.
- Шаг 6. Финальная проверка
Запустить все сервисы;
Убедиться, что контейнеры стартуют без ошибок;
Проверить взаимодействие сервисов между собой.


Критерии оценки
- Соответствие структуры проекта примеру с лекции;
- Корректность docker-compose.yml;
- Выполнение всех требований к сервисам;
- Использование volumes и env_file;
- Возможность воспроизводимого запуска проекта.


## Быстрый старт

1) Создайте локальный `.env` на основе шаблона:

```bash
cp .env.example .env
```

2) Запустите сервисы:

```bash
docker compose up --build
```

- Nginx: http://localhost (порт 80)
- RabbitMQ UI: http://localhost:15672 (логин/пароль из .env)
- Postgres: localhost:5432

> В репозиторий коммитим только `.env.example`, файл `.env` должен оставаться локальным и находится в `.gitignore`.

![](img/ex-2-1.png)
![](img/ex-2-2.png)
![](img/ex-2-3.png)
![](img/ex-2-4.png)
![](img/ex-2-5.png)
![](img/ex-2-6.png)




------


# Задание №3 

- Подключить к объектной модели приложения  (созданной на прошлом уроке) базу данных с использованием ORM фреймворка.
- Протестировать работоспособность системы, а именно создание пользователей, пополнение баланса, списание кредитов с баланса, получение истории транзакций и т.д.
- Подготовить сценарий инициализации базы данных стандартными данными (демо пользователь, демо администратор, базовые модели доступные для работы и т.д.).


## Практическое задание №3: Подключение базы данных и ORM
Контекст задания

Данное задание является продолжением задания по проектированию объектной модели. К этому моменту у студента уже должна быть:
- спроектирована объектная модель предметной области;
- описаны основные сущности и их связи;
- заложена бизнес-логика сервиса.

Теперь требуется связать объектную модель с реальным хранилищем данных с использованием ORM-фреймворка.


Цель работы — научиться:
- применять ORM для отображения объектной модели на реляционную базу данных;
- проектировать таблицы на основе доменных сущностей;
- обеспечивать сохранность и целостность данных;
- проверять бизнес-логику через работу с БД.

После выполнения задания студент должен понимать:
- как объектная модель превращается в структуру БД;
- как ORM управляет сохранением и загрузкой данных;
- какие операции должны быть атомарными и транзакционными.

Общие требования

При выполнении задания необходимо:
- использовать объектную модель, разработанную на первом уроке;
- не изменять бизнес-смысл сущностей без обоснования;
- использовать ORM-фреймворк (например: SQLAlchemy, SQLModel);
- использовать реляционную базу данных (PostgreSQL рекомендуется);
- хранить конфигурацию подключения к БД в переменных окружения.

Требования к моделям и БД

1. Отображение сущностей в БД
Для каждой ключевой сущности объектной модели должна быть:
- соответствующая таблица в базе данных;
- первичный ключ;
- корректные типы данных;
- связи между таблицами (Foreign Key).

Минимально должны быть реализованы таблицы:
- пользователей;
- ML-моделей;
- истории ML-запросов / предсказаний.

2. Работа с пользователями
Необходимо реализовать и протестировать:
- создание пользователя;
- загрузку пользователя из БД;
- связь пользователя с балансом и историей.

3. Работа с балансом и транзакциями
Требуется реализовать:
- пополнение баланса;
- списание кредитов;
- проверку баланса перед списанием;
- сохранение каждой операции в истории транзакций.

4. История запросов пользователя
Необходимо обеспечить возможность:
- получения истории предиктов пользователя;
- сортировки по дате;
- отображения суммы и связанной сущности (ML-задачи, если применимо).

Инициализация базы данных 
Требуется подготовить сценарий начальной инициализации БД, который создаёт:
- демо-пользователя;
- начальный баланс для пользователей;
- набор базовых ML-моделей, доступных для работы.

Инициализация должна:
- выполняться автоматически;
- быть идемпотентной (повторный запуск не ломает данные);

Пошаговый план выполнения задания

Шаг 1. Подключение ORM
- Подключить ORM к проекту;
- Настроить соединение с БД через env-переменные.
Шаг 2. Описание ORM-моделей
- Описать таблицы на основе объектной модели;
- Настроить связи между сущностями.
Шаг 3. Реализация бизнес-операций
- Создание пользователей;
- Пополнение и списание баланса;
- Запись транзакций.
Шаг 4. Инициализация данных
- Реализовать init-скрипт;
- Создать демо-данные.
Шаг 5. Тестирование
- Протестировать все основные сценарии;
- Проверить корректность данных в БД.

Критерии оценки
- Корректность отображения объектной модели в БД;
- Использование ORM, а не сырого SQL;
- Наличие и качество init-данных;
- Успешное тестирование всех сценариев.

Результат: объектная модель, связанная с реальной базой данных через ORM, с корректной историей операций и готовой инициализацией данных для дальнейшей разработки.



ПРОВЕРКА

```
cp .env.example .env
docker compose down -v
docker compose build app
docker compose up -d database
docker compose logs -f database
make init-db
make show-demo
make test

```

![](img/ex-3-1.png)


```
make psql

\dt

select email, role, balance from users order by email;
select name, version, price_per_row, is_active from ml_models order by name, version;
select user_id, tx_type, amount, status, created_at from transactions order by created_at desc limit 20;

select user_id, model_id, status, charged, created_at
from prediction_history
order by created_at desc
limit 20;

\q

docker compose down 
```


![](img/ex-3-2.png)



-----------


# Задание №4

Реализовать интерфейс взаимодействия с системой - REST на FastAPI. Интерфейс должен содержать весь базовый функционал системы: регистрация, авторизация, отправка данных для предсказания, просмотр и пополнение баланса, просмотр истории запросов на предсказания.

Дополнительное задание (не обязательное к выполнению): Реализовать дополнительный интерфейс в виде Telegram bot (библиотека на ваш выбор).


## Практическое задание №4: REST API для ML-сервиса (FastAPI)

Контекст задания

Данное задание является следующим этапом после подключения базы данных и ORM. К этому моменту у студента уже должны быть:
- объектная модель предметной области;
- рабочая база данных;
- реализованная бизнес-логика (пользователи, баланс, ML-запросы).

Задача текущего этапа — предоставить внешний интерфейс взаимодействия с системой в виде REST API.

Цель работы — научиться:
- проектировать REST API поверх существующей бизнес-логики;
- использовать FastAPI для построения Web-сервисов;
- реализовывать аутентификацию и авторизацию (базовая версия, как на лекции);
- обеспечивать сквозные пользовательские сценарии через API.

После выполнения задания студент должен понимать:
- как backend-система открывается внешним клиентам;
- как REST API связывает UI, ботов и другие сервисы с ядром системы;
- как обрабатывать ошибки и граничные случаи на уровне API.

Общие требования

При выполнении задания необходимо:
- использовать FastAPI;
- опираться на существующую объектную модель и ORM;
- не дублировать бизнес-логику в контроллерах;
- хранить конфигурацию в переменных окружения;
- возвращать данные в формате JSON.

Базовый функционал REST API

REST API должно предоставлять весь основной функционал системы.

1. Регистрация и авторизация

Необходимо реализовать:
- регистрацию пользователя;
- авторизацию пользователя;
- механизм аутентификации (например, JWT мы реализуем позже);

2. Работа с балансом

API должно позволять:
- получать текущий баланс пользователя;
- инициировать пополнение баланса;
- возвращать обновлённый баланс после операции.
- Эквайринг подключать не требуется.

3. Отправка данных для ML-предсказаний

Пользователь должен иметь возможность:
- отправить данные для предсказания;
- получить результат предсказания;
- получить ошибку при некорректных данных;
- получить ошибку при недостаточном балансе.

При успешном выполнении запроса:
- с баланса списываются кредиты;
- операция фиксируется в истории.

4. История ML-запросов и транзакций

API должно предоставлять:
- список выполненных ML-запросов;
- историю транзакций пользователя;
- информацию о дате, статусе и списанных кредитах.
- Валидация и обработка ошибок

Требуется:
- валидация входных данных (Pydantic);
- корректные HTTP-коды ответов;
- информативные сообщения об ошибках;
- единый формат ошибок.

Рекомендуемая структура API

Пример логических групп эндпоинтов:
- /auth — регистрация и авторизация;
- /users — данные пользователя;
- /balance — работа с балансом;
- /predict — ML-запросы;
- /history — история операций.

Конкретные URL и схемы — на усмотрение студента.

Дополнительное задание (необязательное)

Реализовать альтернативный интерфейс взаимодействия с системой в виде Telegram-бота:
- бот использует REST API сервиса;
- поддерживает базовые операции:авторизация пользователя;отправка данных для предсказания;просмотр баланса;
- библиотека для Telegram — на выбор студента.

Пошаговый план выполнения задания

Шаг 1. Подготовка FastAPI-проекта
- Создать приложение FastAPI;
- Подключить зависимости.

Шаг 2. Реализация авторизации
- Регистрация пользователя;

Шаг 3. Реализация бизнес-эндпоинтов
- Баланс;
- ML-предсказания;
- История операций.

Шаг 4. Валидация и ошибки
- Добавить Pydantic-схемы;
- Проверить ошибки и коды ответов.

Шаг 5. Тестирование API
- Протестировать через Swagger UI;
- Проверить основные сценарии.

Шаг 6. (Дополнительно) Telegram-бот
- Подключить бота;
- Реализовать базовые команды.

Критерии оценки
- Полнота реализованного REST API;
- Корректная интеграция с бизнес-логикой;
- Качество валидации и обработки ошибок;
- Читаемость и структура кода.

Результат: REST API на FastAPI, предоставляющее полный доступ к функционалу ML-сервиса и готовое к использованию Web-интерфейсом, ботами и внешними клиентами.





ПРОВЕРКА

```
cp .env.example .env
docker compose down -v
docker compose up -d
docker compose build --no-cache app
make init-db
docker compose logs -f app

```


1. Smoke-тест
```
curl -i http://localhost/openapi.json | head
```

![](img/ex-4-1.png)

2. Успешная регистрация

POST /auth/register

Пример тела:
```
{
  "email": "test1@demo.local",
  "password": "StrongPass123!"
}
```

Ответ - 201 или 200 (как у вас заведено)

Response JSON (примерная форма, без password_hash)

2.2 Email уже существует

Ответ

409 Conflict (или 400)

![](img/ex-4-3.png)

2.3 Слишком короткий пароль или пустой

Ответ - 422 или 400

![](img/ex-4-4.png)

2.4 Отсутствует поле email или password

Expected

Ответ - 422

в деталях “Field required” для password

![](img/ex-4-5.png)

2.5 Лишние поля в запросе

Ответ - либо 422 (если extra="forbid") / - либо 200/201 и поле role игнорируется (если extra разрешены)

Проверка - пользователь не становится ADMIN из body

![](img/ex-4-6.png)

3. Успешный логин

POST /auth/login

ОБЯЗАЕТЛЬНО СОХРАНИТЬ access token

![](img/ex-4-7.png)

3.1 Неверный пароль

![](img/ex-4-8.png)

3.2 Пользователь не существует

![](img/ex-4-9.png)

3.3 Пустые/отсутствующие поля

![](img/ex-4-10.png)

4. Баланс

GET /balance/

ВСТАВЛЯЕМ access token 

![](img/ex-4-11.png)

![](img/ex-4-12.png)


5. POST /predict/ — инференс модели

пока отдает 422 тк модели не существет

![](img/ex-4-13.png)


6. GET /users/me — профиль текущего пользователя (нужен токен)

![](img/ex-4-14.png)

7. POST /balance/topup — пополнение баланса

![](img/ex-4-15.png)

8. GET /history/predictions?limit=50 — история предиктов

![](img/ex-4-16.png)

9. GET /history/transactions?limit=50 — история транзакций

![](img/ex-4-17.png)



---------


# Задание №5
Реализовать взаимодействие системы с ML сервисом посредством передачи сообщений.

Требуется развернуть несколько воркеров, подключить их к RabbitMQ, настроить RabbitMQ в режиме один издатель - несколько слушателей.

Воркеры должны получать ML задачу, проводить валидацию данных, выполнять предикт, записать результат своей работы.

Требуется протестировать работу системы вручную на примере Telegram bot или Rest API. 



## Практическое задание: взаимодействие с ML‑сервисом через RabbitMQ

Цель работы — на практике освоить асинхронное взаимодействие сервисов с использованием брокера сообщений RabbitMQ, а также понять, как масштабировать обработку ML‑задач с помощью нескольких воркеров.

В результате выполнения задания студент должен:
- понять модель publisher → broker → consumers;
- уметь настраивать RabbitMQ для сценария «один издатель — несколько слушателей»;
- реализовать ML‑воркеры, обрабатывающие задачи из очереди;
- протестировать систему через внешний интерфейс (REST API или Telegram‑бот).

Общая архитектура

Система состоит из следующих компонентов:
- Publisher (издатель)принимает входящий запрос (REST API или Telegram‑бот);формирует ML‑задачу;публикует сообщение в очередь RabbitMQ.
- принимает входящий запрос (REST API или Telegram‑бот);
- формирует ML‑задачу;
- публикует сообщение в очередь RabbitMQ.
- RabbitMQброкер сообщений;одна очередь;несколько consumers (воркеров), подписанных на эту очередь.
- брокер сообщений;
- одна очередь;
- несколько consumers (воркеров), подписанных на эту очередь.
- ML‑воркеры (Consumers)получают задачи из очереди;валидируют входные данные;выполняют ML‑предикт (можно использовать mock‑модель);сохраняют или логируют результат обработки.
- получают задачи из очереди;
- валидируют входные данные;
- выполняют ML‑предикт;
- сохраняют результат обработки в базе данных (Через REST или напрямую).

Требования к реализации
1. RabbitMQ
- RabbitMQ должен быть развернут:либо локально;либо через Docker / Docker Compose.
- либо локально;
- либо через Docker / Docker Compose.
- Использовать одну очередь.
- Тип обменника (exchange) — direct или default.
- Настроить режим один publisher — несколько consumers.
- Убедиться, что сообщения распределяются между воркерами (round‑robin).

2. Формат сообщения (ML‑задача)
- Сообщение должно передаваться в формате JSON и содержать:
```
{
"task_id": "uuid",
"features": {
"x1": 1.2,
"x2": 5.7
},
"model": "demo_model",
"timestamp": "2026-01-01T12:00:00"
}
```

Минимальные требования:
- task_id — уникальный идентификатор задачи;
- features — входные данные для ML;
- model — имя модели (строка).

3. ML‑воркеры

Необходимо реализовать несколько воркеров (минимум 2), которые:
- Подключаются к RabbitMQ.
- Подписываются на очередь.
- Для каждого сообщения выполняют следующие шаги:
```
{
"task_id": "uuid",
"prediction": 6.9,
"worker_id": "worker-1",
"status": "success"
}
```


4. Сохранение результата:

- запись в БД через REST интерфейс
- запись в БД напрямую

5. Publisher (точка входа)

Необходимо реализовать:
- REST API
- Endpoint POST /predict;
- Принимает JSON с входными данными;
- Генерирует task_id;
- Отправляет сообщение в RabbitMQ;
- Возвращает клиенту task_id.
- Пошаговый план выполнения задания

Шаг 1. Развернуть RabbitMQ
- Запустить RabbitMQ;
- Проверить доступ к management‑панели;
- Создать очередь.
Шаг 2. Реализовать Publisher
- Выбрать интерфейс (REST );
- Реализовать отправку сообщений в очередь;
- Проверить, что сообщения появляются в RabbitMQ.
Шаг 3. Реализовать одного воркера
- Подключиться к очереди;
- Получить сообщение;
- Распарсить JSON;
- Вывести данные в лог.
Шаг 4. Добавить валидацию и ML‑логику
- Проверить структуру входных данных;
- Реализовать простую модель или mock‑предикт;
- Сформировать результат обработки.
Шаг 5. Масштабирование
- Запустить минимум 2 воркера;
- Убедиться, что задачи распределяются между ними.
Шаг 6. Тестирование
- Отправить несколько задач подряд;

Проверить:корректность обработки;распределение нагрузки;отсутствие потери сообщений.
корректность обработки;
распределение нагрузки;
отсутствие потери сообщений.




ПРОВЕРКА

```
cp .env.example .env
docker compose down -v
docker compose up -d --build
docker compose logs --tail=80 ml-worker
docker compose logs --tail=80 ml-worker-2
docker compose ps 
make init-db
make test
make show-demo

```

![](img/ex-5-1.png)
![](img/ex-5-2.png)
![](img/ex-5-3.png)



### RabbitMQ: очередь + 2 consumers

1. Management UI 

![](img/ex-5-5.png)

2. Очередь существует

![](img/ex-5-6.png)

3. Подписано 2 воркера

![](img/ex-5-7.png)



### Swagger

![](img/ex-5-4.png)

1. http://localhost/auth/register


```json
{
  "email": "task5@test.ru",
  "password": "123456"
}
```

![](img/ex-5-8.png)

2. http://localhost/auth/login


```json
{
  "email": "task5@test.ru",
  "password": "123456"
}
```

![](img/ex-5-9.png)

СОХРАНИТЬ access_token

3. http://localhost/users/me

![](img/ex-5-10.png)

4. http://localhost/balance/

![](img/ex-5-11.png)

5. http://localhost/balance/topup

```json
{
  "amount": 100
}
```

![](img/ex-5-12.png)

6. http://localhost/history/transactions


![](img/ex-5-13.png)

7. http://localhost/predict/

найти доступные модели

```bash
docker compose exec database psql -U app_user -d app_db -c "select name, version, is_active from ml_models;"
```

```json
{
  "model_name": "catboost-churn",
   "model_version": "1.0",
  "rows": [
    {"x1": 1.2, "x2": 5.7}
  ]
}
```


![](img/ex-5-14.png)

![](img/ex-5-15.png)


![](img/ex-5-16.png)

![](img/ex-5-17.png)


ЗАКИДАЛА МНОГО ЗАПРОСОВ ЗА РАЗ

![](img/ex-5-18.png)


8. http://localhost/history/predictions

![](img/ex-5-19.png)

-------------


# Задание №6

Разработать Web интерфейс приложения, которое будет взаимодействовать с ранее построенным ядром системы и реализует весь требуемый функционал из общего задания курса.



## Практическое задание №6: Web-интерфейс личного кабинета ML-сервиса

Данное задание является логическим продолжением предыдущих работ курса. К этому моменту у студентов уже должно быть реализовано:
- ядро системы;
- взаимодействие с ML-сервисом (в том числе через RabbitMQ / REST);
- базовая бизнес-логика обработки ML-запросов.

В рамках задания №6 необходимо разработать Web-интерфейс (личный кабинет пользователя), который будет работать поверх уже существующего backend-ядра и предоставлять пользователю полный функционал ML-сервиса.

Цель задания

Цель работы — научиться проектировать и реализовывать пользовательский Web-интерфейс, который:
- взаимодействует с backend-системой;
- управляет пользовательскими данными и балансом;
- позволяет выполнять ML-запросы;
- отображает результаты и историю операций.

После выполнения задания студент должен понимать:
- как Web-интерфейс взаимодействует с backend через API;
- как связываются пользовательские действия и бизнес-логика;
vкак реализуется сквозной пользовательский сценарий от UI до ML-предикта.

Общая концепция приложения

Web-приложение представляет собой личный кабинет пользователя ML-сервиса, в котором пользователь может:
- ознакомиться с возможностями сервиса;
- управлять своим аккаунтом и балансом;
- загружать данные и получать ML-предсказания;
- просматривать историю операций.
- Frontend должен работать поверх существующего backend API, без дублирования бизнес-логики.

Функциональные требования
1. Главная страница
- Описание возможностей ML-сервиса;
- Краткое объяснение, какие задачи решает сервис;
- Статическая информация (допускается без авторизации).
2. Регистрация и авторизация
- Возможность регистрации нового пользователя;
- Возможность авторизации;
- После авторизации пользователь получает доступ к личному кабинету;
- Допускается использование:JWT;
3. Баланс пользователя
- Отображение текущего баланса (в условных кредитах);
- Баланс хранится и управляется на стороне backend;
- Web-интерфейс только отображает и инициирует операции.
4. Пополнение баланса
- Кнопка «Пополнить баланс»;
- Эквайринг реализовывать не требуется;
- Допускается:фиксированная сумма пополнения;ввод суммы вручную;
- После пополнения баланс обновляется.
5. Выполнение ML-запросов из Web-интерфейса

Пользователь должен иметь возможность:
- загрузить данные (форма или файл);
- отправить запрос к ML-сервису;
- при успешном выполнении:получить результат предсказания;увидеть списание кредитов с баланса.

Важно:
- списание кредитов происходит только при успешном выполнении запроса;
- Web-интерфейс обязан отображать ошибки, возвращаемые backend.

6. Валидация данных

Требуется предусмотреть:
- валидацию входных данных;
- если часть данных некорректна:ошибочные данные возвращаются пользователю;корректные данные используются для предсказания;
- пользователь должен явно видеть:какие данные были отклонены;какие — обработаны.

7. Проверка баланса
- При каждом ML-запросе должна выполняться проверка:достаточно ли средств на балансе;
- достаточно ли средств на балансе;
- При отрицательном или нулевом балансе:запрос не выполняется;пользователю возвращается понятное сообщение об ошибке.
- запрос не выполняется;
- пользователю возвращается понятное сообщение об ошибке.

8. История операций и предсказаний

В личном кабинете должна быть страница с историей:
- загруженных данных;
- выполненных ML-запросов;
- полученных предсказаний;
- списанных кредитов.

Для каждой записи отображать:
- дату и время;
- тип операции;
- сумму списания;
- статус выполнения.

9. REST API для ML-запросов

Помимо Web-интерфейса:
- пользователь должен иметь возможность выполнять ML-запросы через REST API;
- Web-интерфейс может использовать тот же API;
- API должен поддерживать:аутентификацию;проверку баланса;валидацию данных.

10. Роль администратора (дополнительно)

Необязательное задание:
 
Администратор имеет возможность:
- просматривать всех пользователей;
- пополнять баланс пользователей;
- модерировать пользовательские пополнения;
- просматривать все транзакции в системе.



Пошаговый план выполнения задания

Шаг 1. Анализ backend API
- Изучить существующие эндпоинты;
- Определить, какие методы нужны Web-интерфейсу.
Шаг 2. Проектирование интерфейса
- Определить основные страницы:главная;регистрация / авторизация;личный кабинет;история операций;
Шаг 3. Реализация базового UI
- Реализовать навигацию;
- Подключить авторизацию;
- Отобразить данные пользователя.
Шаг 4. Работа с балансом
- Отобразить текущий баланс;
- Реализовать пополнение;
- Обновление баланса после операций.
Шаг 5. Интеграция ML-запросов
- Форма отправки данных;
- Отправка запроса в backend;
- Отображение результата или ошибки.
Шаг 6. История и аналитика
- Реализовать страницу истории;
- Отобразить список операций.
Шаг 7. Тестирование
- Проверить основные пользовательские сценарии;
- Проверить обработку ошибок;
- Проверить работу при недостаточном балансе.




ПРОВЕРКА

```
cp .env.example .env
docker compose down -v
docker compose up -d --build

docker compose ps 
make init-db
make test
make show-demo

```


### WEB UI


ГЛАВНАЯ СТРАНИЦА СЕРВИСА

![](img/ex-6-1.png)

ГЛАВНАЯ СТРАНИЦА ЕСЛИ УЖЕ ВОШЕЛ В АККАУНТ

![](img/ex-6-20.png)

ВАЛИДАЦИЯ ПОЧТЫ

![](img/ex-6-2.png)

РЕГИСТРАЦИЯ

![](img/ex-6-3.png)

ЛОГИН

![](img/ex-6-8.png)

ЛИЧНЫЙ КАБИНЕТ

![](img/ex-6-4.png)

ПОПОЛНЕНИЕ БАЛАНСА

![](img/ex-6-5.png)

ВВОД СУММЫ ДЛЯ ПОПОЛНЕНИЯ БАЛАНСА

![](img/ex-6-6.png)

ИСТОРИЯ ТРАНЗАКЦИЙ И ПРЕДИКТОВ

![](img/ex-6-7.png)

КАБИНЕТ АДМИНИСТРАТОРА + ПОПОЛНЕНИЕ ОТ АДМИНА ДРУГИМ ЮЗЕРАМ

![](img/ex-6-9.png)

![](img/ex-6-18.png)

![](img/ex-6-19.png)

ML ЗАПРОС + ПРЕДИКТ + СПИСАНИЕ + ВАЛИДАЦИЯ ДАННЫХ

```json
[
  {"age": 25, "income": 60000},
  {"age": 31, "income": 85000},
  {"age": 42, "income": 110000},
  {"age": 36, "income": 78000},
  {"age": 29, "income": 95000},
  {"age": 50, "income": 130000},
  {"age": 27, "income": 72000},
  {"age": 48, "income": 125000},
  {"age": 39, "income": 99000},
  {"age": 44, "income": 105000}
]
```


![](img/ex-6-10.png)

![](img/ex-6-22.png)

![](img/ex-6-23.png)

![](img/ex-6-24.png)


```json
[
  {"age": 33, "income": 120000},
  {},
  "invalid_row",
  {"age": 50}
]
```

![](img/ex-6-12.png)

![](img/ex-6-25.png)

![](img/ex-6-26.png)

![](img/ex-6-27.png)

```json
[
  {},
  "",
  null
]
```

![](img/ex-6-14.png)

![](img/ex-6-27.png)

![](img/ex-6-28.png)

НЕДОСТАТОЧНО СРЕДСТВ НА БАЛАНСЕ

![](img/ex-6-17.png)

ИСТОРИЯ ТРАНЗАКЦИЙ И ПРЕДИКТОВ ПОСЛЕ ML запросов

![](img/ex-6-16.png)

![](img/ex-6-29.png)


### Swagger

Web использует тот же API, бизнес-логика не дублируется

http://localhost/predict/[id]

![](img/ex-6-30.png)


------------


# Задание №7

Протестировать работоспособность системы, а именно создание пользователей, пополнение баланса, списание кредитов с баланса, получение истории транзакций и т.д.

## Практическое задание №7: Тестирование работоспособности системы

Контекст задания

Данное задание выполняется на финальном этапе курса, когда уже реализованы:
- объектная модель;
- база данных и ORM;
- REST API на FastAPI;
- Web-интерфейс и/или Telegram-бот (опционально);
- RMQ Worker
- инфраструктура проекта (Docker Compose).
- На этом этапе необходимо проверить, что система работает как единое целое, а не как набор разрозненных компонентов.

Цель задания

Цель работы — научиться:
- тестировать сквозные пользовательские сценарии;
- проверять корректность бизнес-логики;
- выявлять ошибки интеграции между компонентами системы;
- документировать результаты тестирования.

После выполнения задания студент должен понимать:
- какие сценарии являются критичными для системы;
- как проверять корректность работы сервиса с точки зрения пользователя;
- почему тестирование — обязательная часть разработки.

Объект тестирования

Тестированию подлежит вся система целиком, включая:
- REST API;
- бизнес-логику;
- работу с базой данных;

Обязательные сценарии тестирования

Необходимо протестировать следующие сценарии.

1. Работа с пользователями
- создание нового пользователя;
- авторизация пользователя;
- повторная авторизация;
- обработка ошибок при неверных данных.
2. Работа с балансом
- получение текущего баланса;
- пополнение баланса;
- корректное обновление баланса после пополнения.
3. Списание кредитов
- успешное списание при выполнении ML-запроса;
- запрет списания при недостаточном балансе;
- отсутствие списания при ошибке ML-запроса.
4. ML-запросы
- успешная отправка данных на предсказание;
- получение результата предсказания;
- обработка некорректных входных данных;
- корректная обработка частично валидных данных (если реализовано).
5. История операций
- сохранение истории транзакций;
- сохранение истории ML-запросов;
- корректное отображение

Пошаговый план выполнения задания

Шаг 1. Подготовка окружения
- Запустить систему через Docker Compose;
- Убедиться, что все сервисы запущены.
Шаг 2. Тестирование пользователей
- Создать пользователя;
- Выполнить авторизацию;
- Получить токен доступа.
Шаг 3. Тестирование баланса
- Получить начальный баланс;
- Выполнить пополнение;
- Проверить обновлённый баланс.
Шаг 4. Тестирование ML-запросов
- Отправить корректные данные;
- Проверить списание кредитов;
- Отправить некорректные данные;
- Проверить обработку ошибок.
Шаг 5. Проверка истории
- Получить историю транзакций;
- Получить историю ML-запросов;
- Сопоставить операции с предыдущими действиями.

Критерии оценки
- Полнота покрытия обязательных сценариев;
- Логичность и последовательность тестирования;
- Корректность выводов;
- Понимание бизнес-логики системы;

## Как запускать

```bash
cp .env.example .env
docker compose down -v
docker compose up -d --build
```

### Посмотреть покрытие


```bash
cd app
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install pytest-cov


BASE_URL=http://localhost pytest -q \
  --cov=src \
  --cov-report=term-missing \
  --cov-report=html


open htmlcov/index.html
```


![](img/ex-7-1.png)

![](img/ex-7-2.png)

![](img/ex-7-3.png)

![](img/ex-7-4.png)

### ТЕСТЫ

![](img/ex-7-5.png)

![](img/ex-7-6.png)



---

# Описание всех 28 тестов

## UNIT-ТЕСТЫ (3 шт.)

| № | Тест                                            | Тип  | Что проверяет                       | Как проверяет                                             | Критичные проверки                                      |
| - | ----------------------------------------------- | ---- | ----------------------------------- | --------------------------------------------------------- | ------------------------------------------------------- |
| 1 | `test_billing.py::test_top_up_and_charge`       | Unit | Базовую логику биллинга             | Вызывает методы пополнения и списания напрямую (без HTTP) | Баланс увеличивается при top-up, уменьшается при charge |
| 2 | `test_billing.py::test_charge_fail`             | Unit | Отказ списания при нехватке средств | Пытается списать больше, чем есть на балансе              | Возвращается статус `rejected`, баланс не меняется      |
| 3 | `test_history.py::test_prediction_history_list` | Unit | Получение истории предиктов         | Создаются записи в истории, затем читается список         | Возвращается список корректной структуры                |

---

## E2E — Пользователи (9 тестов)

| №  | Тест                                                           | Что тестирует         | Сценарий              | Проверка           |
| -- | -------------------------------------------------------------- | --------------------- | --------------------- | ------------------ |
| 4  | `test_register_user_success`                                   | Регистрация           | POST `/auth/register` | 201/200            |
| 5  | `test_login_user_success`                                      | Авторизация           | POST `/auth/login`    | 200 + access_token |
| 6  | `test_relogin_user_success`                                    | Повторная авторизация | Повторный login       | 200                |
| 7  | `test_register_invalid_payload_returns_error[payload0]`        | Пустой payload        | POST `{}`             | 422/400            |
| 8  | `...payload1`                                                  | Нет email             | POST без email        | 422/400            |
| 9  | `...payload2`                                                  | Нет password          | POST без password     | 422/400            |
| 10 | `...payload3`                                                  | Слабый пароль         | POST слабый password  | 422/400            |
| 11 | `test_login_invalid_credentials[no-such-user...]`              | Несуществующий user   | Login                 | 401                |
| 12 | `test_login_invalid_credentials[never-registered...WrongPass]` | Неверный пароль       | Login                 | 401                |
| 13 | `test_register_duplicate_user`                                 | Дубликат              | Два раза регистрация  | 409                |

---

## E2E — Баланс (5 тестов)

| №  | Тест                                                   | Что тестирует          | Сценарий         | Проверка          |
| -- | ------------------------------------------------------ | ---------------------- | ---------------- | ----------------- |
| 14 | `test_balance_get_and_topup`                           | Получение и пополнение | GET → POST → GET | Баланс увеличился |
| 15 | `test_topup_validation[amount0]`                       | Нельзя 0               | POST 0           | 400               |
| 16 | `test_topup_validation[amount1]`                       | Нельзя отрицательное   | POST -10         | 400               |
| 17 | `test_topup_invalid_amount_does_not_change_balance[0]` | Инвариант баланса      | before/after     | Не изменился      |
| 18 | `...[-1]`                                              | То же                  | -1               | Не изменился      |
| 19 | `...[-10]`                                             | То же                  | -10              | Не изменился      |

---

## E2E — ML и списание (4 теста)

| №  | Тест                                                          | Что тестирует                  | Сценарий                    | Проверка                                              |
| -- | ------------------------------------------------------------- | ------------------------------ | --------------------------- | ----------------------------------------------------- |
| 20 | `test_predict_sync_success_charges_credits_and_saves_history` | Успешный ML                    | topup → predict             | 200, charged > 0, баланс уменьшился, запись в истории |
| 21 | `test_predict_sync_no_charge_on_invalid_rows`                 | Нет списания при ошибке данных | predict invalid rows        | charged == 0, баланс тот же                           |
| 22 | `test_predict_sync_not_enough_credits_forbids_charge`         | Недостаточный баланс           | predict при малом балансе   | 402, баланс не меняется                               |
| 23 | `test_predict_sync_unknown_model_returns_404`                 | Неизвестная модель             | predict с плохим model_name | 404, списания нет                                     |

---

## E2E — История (2 теста)

| №  | Тест                                                                   | Что тестирует                  | Сценарий                  | Проверка                        |
| -- | ---------------------------------------------------------------------- | ------------------------------ | ------------------------- | ------------------------------- |
| 24 | `test_transactions_include_topup_and_charge_after_success_predict`     | Корректная история транзакций  | topup → predict → history | Есть `topup` и `charge applied` |
| 25 | `test_unknown_model_does_not_charge_and_does_not_write_applied_charge` | История без applied при ошибке | predict unknown → history | Нет applied списания            |

---

## E2E — Дополнительные (3 теста)

| №  | Тест                                                    | Что тестирует                   | Сценарий                            | Проверка               |
| -- | ------------------------------------------------------- | ------------------------------- | ----------------------------------- | ---------------------- |
| 26 | `test_repeat_authorization_is_ok`                       | Повторная авторизация           | login повторно                      | 200                    |
| 27 | `test_token_can_be_reused_multiple_times`               | Повторное использование токена  | Несколько GET с одним Bearer        | Все 200                |
| 28 | `test_rejected_charge_is_saved_when_not_enough_credits` | rejected записывается в историю | predict при малом балансе → history | Есть `charge rejected` |

---


